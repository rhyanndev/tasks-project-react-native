/**
 * @format
 */

import {AppRegistry} from 'react-native';
import Navigtor from './src/Navigator';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => Navigtor);
